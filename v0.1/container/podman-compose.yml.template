version: "3"
services:
  file-server:
    container_name: file-server__rust_{port}
    build:
      context: ./
      dockerfile: file-server.podmanfile
    ports:
      - {port}:{port}
    volumes:
      - {root_dir}:/usr/share/www:z
