// script that reads json file

// uses podman-compose.yml.template as a template
// uses podmanfile.template as a template

// outputs templates at a directory

// then a user just needs to
// pomdan-compose -f ./podman-compose.yml build


cargo run ./ ../../../example.json ../../../demo

get first argument

load config

make directory and filepaths absolute

create 

get second argument

get absolute value of second argument

// two filepaths? 

format podmanfile

save podmanfile to directory

format podman-compose

save podman-compose to directory
